#include "main.h"

/**
 * main - tests positive_or_negative() and outputs for case 0
 *
 * Description: tests positive_or_negative() and outputs for case 0
 * Return: Always 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
