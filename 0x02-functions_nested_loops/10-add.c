#include "main.h"

/**
 * add - adds two integers and returns the result
 * @x: first parameter
 * @y: second parameter
 *
 * Description: adds two integers and returns the result
 * Return: Returns the result of the sum of 2 integers
 */
int add(int x, int y)
{
	int sum = x + y;

	return (sum);
}
