#include <stdio.h>

/**
 * main - prints all single digit numbers
 *
 * Description: prints base 10 numbers of starting from 0
 * Return: Always 0
 */
int main(void)
{
	int i = 0;

	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
